{
  "name": "resume-ats-builder-root",
  "version": "1.0.0",
  "description": "Root workspace runner for Resume Builder & ATS Score Calculator",
  "scripts": {
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run server\" \"npm run client\""
  }
}
