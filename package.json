{
  "name": "cv-audit-root",
  "version": "1.0.0",
  "description": "Root workspace runner for CVAudit",
  "scripts": {
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "npx concurrently \"npm run server\" \"npm run client\""
  }
}
